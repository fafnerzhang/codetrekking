# Example environment variables for PeakFlow
# Copy this file to .env and fill in the actual values as needed.

# === Encryption Configuration ===
# Encryption key for Garmin credentials (Base64-encoded 32 bytes)
# Generate with: python -c "import secrets, base64; print(base64.b64encode(secrets.token_bytes(32)).decode())"
GARMIN_ENCRYPTION_KEY=your-base64-encoded-32-byte-key-here

# Encryption version (integer, default: 1)
GARMIN_ENCRYPTION_VERSION=1

# === Elasticsearch Configuration ===
# Elasticsearch host URL
ELASTICSEARCH_HOST=http://localhost:9200

# Elasticsearch authentication credentials
ELASTICSEARCH_USER=elastic
ELASTICSEARCH_PASSWORD=your-elasticsearch-password

# === Garmin Data Storage ===
# Base directory for Garmin data storage (defaults to ~/storage/garmin)
DEFAULT_GARMIN_CONFIG_DIR=/home/user/storage/garmin
